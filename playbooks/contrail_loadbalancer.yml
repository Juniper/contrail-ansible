---
- name: "Setup Contrail Loadbalancer"
  hosts: contrail-loadbalancer

  pre_tasks:
    - include_vars: contrail_loadbalancer.yml
      tags: always

  roles:
    - role: common

    - role: contrail/loadbalancer
      tags: [contrail.loadbalancer]
