---
contrail_compute_mode: bare_metal
enable_openstack_compute: "{{ true if (provision_type | default('')) != 'ISSU' else false }}"
enable_lbaas: true
deployment_platform: docker
no_upstart: "{{ true if deployment_platform == 'docker' }}"
#Tor Agent defaults
tor_agent: {}
tor_agent_list: []
kernel_version_installed: ""
