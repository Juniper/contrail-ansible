neutron_protocol: http
nova_password: "{{ keystone_admin_password }}"
neutron_password: "{{ keystone_admin_password }}"
service_tenant_name: "{{ keystone_admin_tenant }}"
region: RegionOne
openstack_ip: "{{ keystone_ip }}"
neutron_port: 9696
